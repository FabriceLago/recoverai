export const environment = {
  production: false,
  // Populated from .env at build time via scripts/set-env.ts, or paste local Supabase project values here for dev only — never commit real keys.
  supabaseUrl: '',
  supabaseAnonKey: '',
};
